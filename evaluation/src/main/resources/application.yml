spring:
  application:
    name: evaluation
  profiles:
    active: dev
mybatis:
  mapper-locations: classpath:org.nix.lovedomain.dao.business/*Mapper.xml
  mappers: org.nix.lovedomain.dao.base.BaseBusinessMapper
  configuration:
    cache-enabled: false
logging:
    level:
      org.nix.lovedomain.dao: debug